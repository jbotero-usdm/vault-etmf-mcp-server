export default async function handler(req, res) {
  res.status(200).json({
    status: 'ok',
    generatedAt: new Date().toISOString(),
    note: 'Use /api/intake, /api/vault-summary, and /api/health for live data; this route is reserved for future aggregated cards.'
  });
}
